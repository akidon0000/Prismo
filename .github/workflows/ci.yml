name: CI

# アプリ本体を触った PR・push だけでユニットテストとリリースビルドを回す。
# 必須ステータスには載せない（パス不一致で未起動だと Expected のまま止まるため）。
on:
  push:
    branches: [main]
    paths:
      - "Prismo/Sources/**"
      - "Prismo/Tests/**"
      - "Package.swift"
      - ".github/workflows/ci.yml"
  pull_request:
    paths:
      - "Prismo/Sources/**"
      - "Prismo/Tests/**"
      - "Package.swift"
      - ".github/workflows/ci.yml"

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Unit Test
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v7

      - name: Show toolchain
        run: |
          swift --version
          xcodebuild -version

      - name: Unit Test
        run: swift test

      - name: Build (Release)
        run: swift build -c release
